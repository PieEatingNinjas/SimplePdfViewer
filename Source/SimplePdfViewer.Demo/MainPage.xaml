<Page
    x:Class="SimplePdfViewer.Demo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimplePdfViewer.Demo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:SimplePdfViewer"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{x:Bind File.Path}" />
        <controls:SimplePdfViewerControl Grid.Row="1" 
            AutoLoad="True"
            Source="{x:Bind Source, Mode=OneWay}"
            File="{x:Bind File, Mode=OneWay}"
            VerticalAlignment="Stretch" 
            HorizontalAlignment="Stretch"
            IsZoomEnabled="true"/>
    </Grid>
</Page>
